import { StyleSheet, Text, View } from 'react-native';
import React from 'react';
import MainLayout from '../layouts/main-layout';

const CreateMatch = () => {
    return (
        <MainLayout>
            <View>
                <Text>CreateMatch</Text>
            </View>
        </MainLayout>
    );
};

export default CreateMatch;

const styles = StyleSheet.create({});